# BLOCKED.md — 待裁决清单

以下均为「顺手活 / 后续增强」，本期需求未要求，**未擅自实现**，待你裁决：

1. **搜索功能**：站内条目全文搜索。需求未含，属顺手活，待裁决。
2. **暗色模式**：**已完成**（2026-08-03，随 Tailwind v4 + shadcn/ui 重写一并实现）。`.dark` 类切换由 `src/theme-provider.tsx` 提供，Header 右上角有 🌙/☀️ 切换按钮，偏好存 localStorage。不再阻塞。
3. **接入真实数据源**：**已完成**（2026-08-03）。默认 `npm run data` 走 `scripts/fetch-real.mjs`：新浪财经滚动新闻（lid=2509）+ 腾讯 gtimg 实时行情（上证/深成/创业板/沪深300/恒生/纳指）。失败时回退 seeds(mock)。详情见 README。如需接第二真实新闻源做多源合并、或换更丰富的行情标的（黄金/美元 gtimg 无可靠免费代码），可再议。
4. **单元测试框架**：需求未要求，且明确要求「顺手活（含加单元测试框架）写 BLOCKED 待裁决」，故未引入。数据正确性由 `scripts/validate-data.mjs` 的判据保证。
5. **npm registry 不可达时的零依赖静态站降级**：本期实测 registry 可达（PONG 913ms），未触发降级。若未来 registry 不可达，需按原拍板降级为「HTML + 原生 JS 纯静态站」（架构/数据管线/验收标准不变）。该降级分支尚未实现，待触发或裁决是否预先实现。

## 自选池模块（2026-09-11 新增）衍生待裁决项
6. **自选池「每日定时快照 / 跌破邮件提醒」**：本期按用户「单独模块」的表述，只做了「打开即拉最新日 K 现算 MA5」的实时仪表盘，**未加定时任务**。若要真正的每日收盘后快照留档、或跌破 5 日线时邮件提醒，需另配 automation 定时任务，待裁决。
7. **自选池扩展港美股**：本期按用户选择**仅支持 A 股**（自动补 sh/sz/bj 前缀）。若要覆盖港股（hk00700）/美股（usAAPL），需扩展代码归一化并验证 gtimg 对应接口，待裁决。
8. **自选池导入方式扩展**：本期只做「粘贴文本」。CSV/JSON 文件导入未做，待裁决。
9. **自选池信号的持久化历史**：当前只展示「当前是否站上 + 今日是否上穿/跌破」，不留历史。若要回看某股哪天突破/跌破，需要历史快照，与第 6 项一起裁决。

## 已采纳的「建议」类决策（在 PROGRESS.md 记录原因）
- 原决策「UI 采用手写 CSS、不上 Tailwind」**已推翻**（2026-08-03）。用户明确要求按 Tailwind v4 + shadcn/ui 规范重写界面样式：已接入 `tailwindcss`+`@tailwindcss/vite`+`tw-animate-css`，采用 shadcn 四步主题架构（`src/index.css` 的 `:root`/`.dark` 变量 + `@theme inline` 映射 + `@layer base`），保留红涨绿跌语义色（`--up`/`--down`），并补齐暗色模式。已验证 `npm run build` 退出码 0、preview 四 URL 200，语义类（bg-background/text-up/text-down/.dark）正确编译。
